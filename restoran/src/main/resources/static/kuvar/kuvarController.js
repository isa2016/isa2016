var app = angular.module('kuvar.controllers', []);

app.controller('kuvarController', [
		'$scope',
		'$window',
		'kuvarService',
		'$location',
		function($scope, $window, kuvarService, $location) {

		} ]);

